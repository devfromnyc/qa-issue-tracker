# MongoDB (MongoDB Atlas recommended for Vercel)
MONGODB_URI=mongodb+srv://user:password@cluster.mongodb.net/qa-issue-tracker

# NextAuth — generate with: openssl rand -base64 32
NEXTAUTH_URL=http://localhost:3000
NEXTAUTH_SECRET=your-secret-here
